# Runtime dependencies for the local ingestion and CLI workflows.
# Python 3.11+ is required. Install with:
#   python -m pip install -r requirements.txt

# OpenAI embeddings, visual descriptions and grounded answers.
openai>=2.41.0,<3
# Local persistent vector store.
chromadb>=1.5.9,<2
# PDF text extraction and page rendering.
PyMuPDF>=1.27.2.3,<2
# Local environment configuration.
python-dotenv>=1.2.2,<2
# Supabase Postgres/pgvector loading.
psycopg[binary]>=3.3.4,<4
